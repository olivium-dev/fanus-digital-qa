[build]
  command = "# no build command needed for now"
  publish = "."
  functions = "netlify/functions"

[dev]
  framework = "#static"
  
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200 